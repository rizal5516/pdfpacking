# ============================================================
# FILE INI HANYA CONTOH — JANGAN COMMIT .env ASLI KE GIT
# Salin file ini menjadi .env lalu isi nilainya
# ============================================================

# === BSrE API Credentials ===
BSRE_AUTH=isi_dengan_nilai_base64_auth_bsre
BSRE_NIK=isi_dengan_nik_entitas
BSRE_PASSPHRASE=isi_dengan_passphrase_bsre

# === BSrE Endpoint (opsional jika ingin override default) ===
# BSRE_TOKEN_URL=https://api-bsre.bssn.go.id/oauth2/token?grant_type=client_credentials
# BSRE_SIGN_URL=https://api-bsre.bssn.go.id/esign/v2/api/entity/sign/hash-v2

# === API Key untuk proteksi endpoint internal ===
# Gunakan string acak yang panjang, minimal 32 karakter
# Contoh generate: openssl rand -hex 32
APP_API_KEY=isi_dengan_api_key_yang_kuat_minimal_32_karakter

# === Limit (opsional, ada default di application.properties) ===
# APP_MAX_FILES_PER_REQUEST=10
# APP_MAX_BASE64_SIZE_CHARS=14000000
